const hre = require("hardhat"); const fs = require("fs"); const path = require("path"); async function main() { const [admin] = await hre.ethers.getSigners(); const contracts = JSON.parse(fs.readFileSync(path.join(__dirname, "../migrationsDeployment/contracts.json"), "utf8")); const ir = contracts.issuerRegistry; const contract = await hre.ethers.getContractAt(ir.abi, ir.address, admin); const tx = await contract.addIssuer("0xcda7138ff88e70364de8c41939f03f3d5334c859"); await tx.wait(); console.log("Granted role to metamask"); } main();
